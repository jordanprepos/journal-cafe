# ── Runtime ──────────────────────────────────────────────
fastapi==0.110.1
uvicorn==0.25.0
pydantic>=2.6.4
python-dotenv>=1.0.1
email-validator>=2.2.0    # EmailStr validation
motor==3.3.1              # async MongoDB driver
pymongo==4.5.0            # pinned to match motor
pyjwt>=2.10.1             # JWT signing (HS256)
bcrypt==4.1.3             # password hashing
slowapi>=0.1.9            # rate limiting

# ── Dev / test ───────────────────────────────────────────
pytest>=8.0.0
requests>=2.31.0          # used by the integration test suite
black>=24.1.1
isort>=5.13.2
flake8>=7.0.0
mypy>=1.8.0
